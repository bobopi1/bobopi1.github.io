window.LatelierShopifyConfig = window.LatelierShopifyConfig || {
  // Optional override. The storefront loads automatically once domain + token are filled in.
  enabled: false,

  // Use your Shopify shop domain, usually in the form "your-store.myshopify.com".
  shopDomain: "https://latelier-9865.myshopify.com/",

  // Use the public storefront access token from the Headless channel.
  storefrontAccessToken: "c4aef8e9c865218160033d19f5cd39ce",

  // Pin a Storefront API version so the storefront stays stable.
  apiVersion: "2026-01"
};
